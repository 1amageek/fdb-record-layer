syntax = "proto3";

package example;

// User record type
message User {
    int64 user_id = 1;
    string name = 2;
    string email = 3;
    int64 age = 4;
}

// Union type for all record types
message RecordTypeUnion {
    oneof record {
        User user = 1;
    }
}
